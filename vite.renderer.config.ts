import { defineConfig } from 'vite';

export default defineConfig({
  // root: 'renderer', // if your entry was renderer.ts in renderer folder
  // // plugins: [react()],
  // build: {
  //   outDir: '../dist/renderer',
  // },
});
